name: flow v.02

on:
  push:
    branches:    
      - feature-new-workflow

env:
  TEST_ENV: "/home/htec11090/testDir"

jobs:
  env_variable_test:
    name: env variable fix v1
    runs-on: self-hosted
    steps:
      - name: Test1
        id: test_1
        run: |
          cd ${{ env.TEST_ENV }}
          pwd
          cat testfile.txt
          MY_STRING="hello"
          echo "MY_STRING=$MY_STRING" >> $GITHUB_OUTPUT
          echo $GITHUB_ENV

      - name: Test2
        run: |
          echo "test2"
          echo "The string is: ${{ env.MY_STRING }}"
          echo "test2222"

      

      
