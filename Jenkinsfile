pipeline {
    agent any 
    options { timestamps () }
    stages {
        stage('Test stage') {
            steps {
                echo 'Hello from jenkinsfile v1' 
            }
        }
    }
}